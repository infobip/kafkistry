app:
  security:
    enabled: false
    csrf-enabled: false
  scheduling.enabled: false
  hazelcast:
    group: "kafkistry-it-test"
    discovery:
      type: NONE
  http:
    root-path: "/kafkistry"
  autopilot:
    cycle:
      after-startup-delay-ms: 0

AUTOPILOT_ENABLED: false
HTTP_REQUESTS_LOGGING_ENABLED: true

